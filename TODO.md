# TODO

- [ ] Add fixed size styling for the capture-thumbnail buttons (capture-thumb) so each thumbnail is consistent.
- [ ] Ensure image sizing inside thumbnail remains correct (object-fit).
- [ ] Run client build/dev lint (if available) to confirm no CSS errors.

